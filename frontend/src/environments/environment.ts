/* 
 * Environment configuration per sviluppo (development)
 * Questo file viene utilizzato quando esegui `ng serve` o `npm start`
 */

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api'
};
